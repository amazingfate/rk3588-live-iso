#!/bin/sh

set -e

if [ -f /usr/sbin/bootloader-config ]
then
	sed -i "s|grub-amd64-efi|grub-arm64-efi|g" /usr/sbin/bootloader-config
	sed -i "s|installEFIFallback: false|installEFIFallback: true|g" /etc/calamares/modules/bootloader.conf
fi
